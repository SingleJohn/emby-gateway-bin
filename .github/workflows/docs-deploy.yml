name: Deploy Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Build VitePress
        run: |
          echo "configure-pages.base_path=${{ steps.pages.outputs.base_path }}"
          echo "configure-pages.base_url=${{ steps.pages.outputs.base_url }}"
          BASE_PATH="${{ steps.pages.outputs.base_path }}"
          if [ -s docs/public/CNAME ]; then
            DOCS_BASE="/"
            echo "Detected docs/public/CNAME, force DOCS_BASE=/ for custom domain."
          elif [ -z "$BASE_PATH" ] || [ "$BASE_PATH" = "/" ]; then
            DOCS_BASE="/"
          else
            DOCS_BASE="${BASE_PATH%/}/"
          fi
          echo "Using DOCS_BASE=$DOCS_BASE"
          DOCS_BASE="$DOCS_BASE" npm run docs:build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
